# Install nginx in your linux system  
Download all the lastest package  
`sudo apt update`

Installl Nnginx only  
`sudo apt install nginx`

Start the nginx service  
`systemstl start nginx`

Happy Learning!!
